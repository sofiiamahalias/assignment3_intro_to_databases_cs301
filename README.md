# assignment3_intro_to_databases_cs301
Task 1. Створила функцію для підрахунку загальної суми замовлення з використанням змінної total для збереження підрахованого результату; в частині begin-end прописую основну логіку обрахунку за формулою, яка використовується, якщо в замовленні є товари. В іншому випадку повертаю 0. В кінці зроблена перевірка роботи функції.

Task 2. Створила процедуру додавання нового замовлення. Спочатку є перевірка чи існує клієнт за id, якщо так - створюється новий запис в orders та вставляються значення, в іншому випадку - замовлення не створюється. Перевірила, чи працює процедура.

Task 3. Створено процедуру, змінні для збереження ціни та кількості. Оскільки в нас є дві вимоги, першу (кількість не від'ємна) прописую спочатку, потім беру з таблиці ціну та значення кількості (без неї не можна зробити другу перевірку), після цього порівнюю бажану кільькість товару з наявною на складі. В кінці вставляю значення в новий рядок та зменшую кількість наявного товару.

Task 4. Оскільки тригер не може працювати без trigger function, я спочатку прописую функцію update_order_total (в якій основна логіка змін, які мають відбутись після insert, update or delete з використанням функції з першої вправи), яка буде викликатись тригером trigger_update_order_total (він спрацьовує у разі додавання, зміни чи видалення; викликає функцію оновлення суми замовлення)

Task 5. Аналогічно четвертій вправі, спочатку створюю функцію add_order_log(), яка вставляє в таблицю order_log вказані в умові значення. Далі створюю тригер (викликається лише після insert в таблиці orders та запускає функцію add_order_log).

Task 6. Коментарі щодо тестування в коді
